# 📘 คู่มือใช้งานกับ Claude Code

คู่มือนี้จะช่วยให้คุณใช้ Claude Code พัฒนาเว็บไซต์คณะแพทยศาสตร์ได้อย่างมีประสิทธิภาพ

---

## 🚀 เริ่มต้นใช้งาน

### ขั้นตอนที่ 1: ติดตั้ง Project

```bash
# แตกไฟล์ zip และเข้าไปในโฟลเดอร์
cd med_npu

# สร้าง virtual environment
python -m venv venv
source venv/bin/activate  # Linux/Mac
# หรือ venv\Scripts\activate  # Windows

# ติดตั้ง dependencies
pip install -r requirements.txt

# สร้าง database
python manage.py migrate

# สร้าง superuser
python manage.py createsuperuser

# รัน server
python manage.py runserver
```

### ขั้นตอนที่ 2: ทดสอบ

- เปิด http://127.0.0.1:8000 ดูหน้าเว็บ
- เปิด http://127.0.0.1:8000/admin เข้า Admin

---

## 🤖 Prompts สำหรับ Claude Code

### 📝 เพิ่มหน้าใหม่

```
สร้างหน้า "ประวัติคณะ" ที่ /about/history/
- มีเนื้อหาแบบ CMS (แก้ไขได้จาก Admin)
- มี sidebar เมนูเกี่ยวกับคณะ
- มี breadcrumb
- responsive design
```

### 👥 เพิ่ม Model ผู้บริหาร

```
เพิ่ม field ใน Executive model:
- bio (TextField) - ประวัติย่อ
- education (TextField) - ประวัติการศึกษา
- specialty (CharField) - ความเชี่ยวชาญ

อัพเดท admin และ template ด้วย
```

### 📰 ปรับปรุงระบบข่าว

```
เพิ่มฟีเจอร์ใน News app:
- Pin ข่าวสำคัญ (is_pinned field)
- กำหนดวันหมดอายุ (expire_date field)
- จัดเรียงข่าว: pinned ก่อน, แล้วตาม published_at
- ซ่อนข่าวที่หมดอายุ
```

### 🔍 เพิ่มระบบค้นหา

```
สร้างระบบค้นหาที่ /search/
- ค้นหาจาก News (title, content)
- ค้นหาจาก Program (name, description)
- แสดงผลแยกหมวดหมู่
- มี pagination
```

### 🌐 เพิ่ม Multi-language

```
เพิ่มระบบ 2 ภาษา (ไทย/English):
- เพิ่ม field _en สำหรับ title, content ใน News
- สร้าง language switcher ใน navbar
- เปลี่ยนภาษาตาม URL (/en/, /th/)
```

### 📊 เพิ่มหน้า Dashboard

```
สร้างหน้า Dashboard สำหรับ Admin:
- แสดงจำนวนข่าว, ผู้บริหาร, หลักสูตร
- กราฟแสดง view count ย้อนหลัง 30 วัน
- รายการข่าวล่าสุด 5 อัน
- รายการ admissions ที่กำลังเปิดรับ
```

### 🗺️ เพิ่ม Google Maps

```
เพิ่ม Google Maps ในหน้า Contact:
- ใช้ Google Maps Embed API
- แสดงตำแหน่งคณะแพทย์
- มี marker และ info window
- responsive
```

### 📧 เพิ่มฟอร์มติดต่อ

```
สร้างฟอร์มติดต่อในหน้า Contact:
- ชื่อ, อีเมล, เบอร์โทร, หัวข้อ, ข้อความ
- validation ทั้ง frontend และ backend
- ส่งอีเมลแจ้งเตือนไปยัง admin
- บันทึกลง database
- แสดง success message
```

### 🎨 ปรับ UI/UX

```
ปรับปรุง Hero Section:
- เพิ่ม background video หรือ image slider
- animation เมื่อ scroll
- ปุ่ม CTA ที่โดดเด่นขึ้น
- รองรับ mobile ได้ดีขึ้น
```

---

## 📂 โครงสร้างที่ควรรู้

```
med_npu/
├── config/settings.py     # ตั้งค่าหลัก (แก้ DB, SECRET_KEY)
├── apps/
│   ├── core/              # หน้าหลัก, search, contact
│   ├── about/             # เกี่ยวกับคณะ, ผู้บริหาร
│   ├── education/         # หลักสูตร, รับสมัคร
│   └── news/              # ข่าวประชาสัมพันธ์
├── templates/
│   ├── base.html          # Layout หลัก
│   ├── components/        # Navbar, Footer (ใช้ร่วมกัน)
│   └── [app]/             # Template แต่ละ app
└── static/                # CSS, JS, Images
```

---

## 🎨 Color Palette

สีหลัก: **#229799** (Teal)

```css
--primary-500: #229799;  /* สีหลัก */
--primary-600: #1B7A7C;  /* Hover */
--primary-700: #155D5E;  /* Active */
--primary-900: #082323;  /* Footer */
```

---

## ⚡ Tips สำหรับ Claude Code

### 1. ใช้ context ให้เพียงพอ
```
ดูไฟล์ apps/news/models.py และ templates/news/news_list.html
แล้วเพิ่มฟีเจอร์ filter ตามหมวดหมู่
```

### 2. ระบุ path ชัดเจน
```
แก้ไขไฟล์ templates/components/navbar.html
เพิ่มเมนู "งานวิจัย" ระหว่าง "การศึกษา" กับ "ข่าวสาร"
```

### 3. ขอให้ทดสอบ
```
สร้าง view และ test ว่า /news/category/announcement/ 
แสดงเฉพาะข่าวหมวด "ประกาศ" ถูกต้อง
```

### 4. ทำทีละขั้นตอน
```
ขั้นตอน 1: สร้าง Model สำหรับ Gallery
ขั้นตอน 2: สร้าง Admin interface
ขั้นตอน 3: สร้าง View และ Template
ขั้นตอน 4: เพิ่มเมนูใน Navbar
```

---

## 🔧 คำสั่ง Django ที่ใช้บ่อย

```bash
# รัน server
python manage.py runserver

# สร้าง migrations หลังแก้ model
python manage.py makemigrations
python manage.py migrate

# สร้าง superuser
python manage.py createsuperuser

# เข้า Django shell
python manage.py shell

# Collect static files (production)
python manage.py collectstatic

# สร้าง app ใหม่
python manage.py startapp appname
mv appname apps/
```

---

## 🚨 สิ่งที่ต้องทำก่อน Production

1. เปลี่ยน `SECRET_KEY` ใน settings.py
2. ตั้ง `DEBUG = False`
3. ตั้งค่า `ALLOWED_HOSTS`
4. เปลี่ยน Database เป็น MySQL
5. ตั้งค่า Static files server
6. ตั้งค่า Media files server
7. ใช้ HTTPS

---

## 📞 ต้องการความช่วยเหลือ?

เพียงบอก Claude Code ว่า:

```
ช่วยดู error นี้หน่อย: [วาง error message]
```

```
อธิบายว่า apps/news/views.py ทำงานยังไง
```

```
ช่วยเขียน test สำหรับ News model
```

---

*Happy Coding! 🎉*
